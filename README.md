# BurstNote-IconBuilder

Tool that provides a clean and build command to compile a set of icon themes in sass.

**fixIconNames**

`node ./dist/fixIconNames --help`

**buildIconStyles**

`node ./dist/buildIconStyle --help`
